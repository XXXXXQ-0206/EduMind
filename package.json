{
  "name": "edumind",
  "private": true,
  "scripts": {
    "security:audit": "npm --prefix backend audit && npm --prefix frontend audit && npm --prefix frontend-legacy audit && npm --prefix services/bilibili-mcp audit",
    "frontend:lint": "npm --prefix frontend run lint",
    "frontend:build": "npm --prefix frontend run build",
    "bilibili:build": "npm --prefix services/bilibili-mcp run build"
  },
  "authors": [
    "nullure (https://github.com/nullure)",
    "recabasic (https://github.com/recabasic)"
  ],
  "engines": {
    "node": ">=22.0.0"
  }
}
